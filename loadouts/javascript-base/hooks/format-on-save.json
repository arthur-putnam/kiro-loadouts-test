{
  "name": "Format on Save",
  "version": "1.0.0",
  "description": "Run Prettier formatting when files are saved",
  "when": {
    "type": "fileEdited",
    "patterns": ["*.js", "*.jsx", "*.ts", "*.tsx", "*.json", "*.md"]
  },
  "then": {
    "type": "askAgent",
    "prompt": "Run `npm run format` to format the edited file with Prettier."
  }
}
